============
Installation
============

At the command line::

    $ easy_install twindb-agent

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv twindb-agent
    $ pip install twindb-agent

